import java.util.*;
public class Thesaurus {
    public ArrayList<TreeSet<String>> convert(String[] s) {
        ArrayList<TreeSet<String>> myList = new ArrayList<>();
        for(int i = 0; i < s.length; i++) {
            TreeSet<String> set = new TreeSet<>();
            ArrayList<String> wordList = String.split(" ", s);
            for(i = 0; i <)
        }
    }
    public String[] edit(String[] entry) {
        
    }
}